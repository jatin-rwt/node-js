const math = require("./math.js")
//const { add, sub} = require("./math.js")

//  ./ means current directory
//  "xyz" means xyz is a built in feature and searches it in the node directory
console.log(math.add(2,3));
console.log(math.sub(2,3));